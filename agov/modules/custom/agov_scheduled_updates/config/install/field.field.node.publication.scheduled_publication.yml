langcode: en
status: true
dependencies:
  config:
    - field.storage.node.scheduled_publication
    - node.type.publication
    - scheduled_updates.scheduled_update_type.scheduled_publication
id: node.publication.scheduled_publication
field_name: scheduled_publication
entity_type: node
bundle: publication
label: 'scheduled publication'
description: ''
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler_settings:
    target_bundles:
      - scheduled_publication
  handler: 'default:scheduled_update'
field_type: entity_reference
